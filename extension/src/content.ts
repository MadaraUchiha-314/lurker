// Content script for Lurker extension
// This is an empty content script as network calls are handled by the service worker

console.log('Lurker content script loaded');

// Export empty object to satisfy TypeScript module requirements
export {}; 